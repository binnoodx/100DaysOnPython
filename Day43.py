# Virtual Environment in Python

# run these commands
# python -m venv myenv ---> for making Virtual environments
# myenv/scripts/activate.bat in Terminal or myenv/scripts/activate.ps1 in Cmd
# then you can install custom packages version which work for that file only
# Use deactivate to close virtual environments
# 
# pip freeze > requirements.txt ----> Give all the required packages in text File
# pip install -r requirements.txt ---> Install those packages which are in txt file
#

